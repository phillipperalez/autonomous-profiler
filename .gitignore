# Bench target (separate repo + large data) — not part of this project
/analyzer/

# Rust build output
**/target/

# Profiler run data (live, regenerated) + caches
autonomous-profiler/data/
**/.ap/

# Frontend
**/node_modules/
**/dist/
**/.vite/

# Trial bench scratch
/tmp/ap-trial-*

# OS / editor
.DS_Store
*.swp

# External polars clone (improver target) — not part of this repo
/polars/
/polars-wt*/
/polars-bench-wt*/
/.swarm-target/

# chat exports land at repo root; keep them out unless intentionally placed in hackday/
/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt
